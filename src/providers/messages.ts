import { defineMessages } from '@openedx/frontend-base';

const messages = defineMessages({
  toastRetryLabel: {
    id: 'instruct.toast.retry.label',
    defaultMessage: 'Retry',
    description: 'Label for the retry action in toast notifications.',
  },
});
export { messages };
